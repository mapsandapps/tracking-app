handle more file types
maybe drop use of gpsbabel?
handling one large file (i.e. how to set Seg <- NA when some amount of time has elapsed)
maybe instead of having first point in each track have Seg <- NA, instead create a new observation before each track with Seg (or Lat or Long) <- NA
GUI
web page
web login
let user choose (input) file location
let user choose (output) file location
make maps interactive (zoom, recenter)